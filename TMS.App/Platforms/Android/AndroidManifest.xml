<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        
        <!-- Declare the intent filter for handling tmsapp://callback -->
        <activity android:name=".YourActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <!-- Specify the URI scheme you want to handle -->
                <data android:scheme="msal58c9ae05-1d48-4f7e-ae04-a0926ae1cb8a" android:host="auth" />
            </intent-filter>
        </activity>
        
    </application>

    <queries>
	  <package android:name="com.azure.authenticator" />
	  <package android:name="com.companyname.tms_app" /> <!-- This value should be copied from the MauiAuthApp.csproj file -->
	  <package android:name="com.microsoft.windowsintune.companyportal" />
	</queries>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
